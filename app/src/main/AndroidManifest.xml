<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jeonjin_il.db">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".FoodSearch_Ref"/>
        <activity android:name=".MainAdmin"/>
        <activity android:name=".FoodListView"/>
        <activity android:name=".FoodSearch_Hash"/>
        <activity android:name=".FoodSearch_Random"/>
        <activity android:name=".MainLogin"/>
        <activity android:name=".ShoppingBasket"/>
    </application>

</manifest>